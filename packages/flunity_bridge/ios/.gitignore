# SwiftPM build outputs (generated when consuming the package)
.build/
.swiftpm/

# Xcode build artifacts
build/
DerivedData/

# CocoaPods
Pods/
Podfile.lock

# Generated by `flutter pub get` in a consuming app — these belong to the
# CONSUMING app's ios/ tree, not the package's ios/ tree.
Flutter/
Runner/
Runner.xcodeproj/
Runner.xcworkspace/
